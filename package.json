{
  "name": "orcid-parser",
  "version": "0.2.0",
  "description": "A TypeScript library for fetching and parsing ORCID v3.0 publication data",
  "type": "module",
  "main": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs"
    },
    "./constants": {
      "types": "./dist/constants.d.ts",
      "import": "./dist/constants.mjs",
      "require": "./dist/constants.cjs"
    }
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && npm run build:types && npm run build:cjs && npm run build:esm",
    "build:cjs": "esbuild src/index.ts src/constants.ts --bundle --platform=node --format=cjs --minify --legal-comments=none --outdir=dist/ --out-extension:.js=.cjs",
    "build:esm": "esbuild src/index.ts src/constants.ts --bundle --platform=node --format=esm --minify --legal-comments=none --outdir=dist/ --out-extension:.js=.mjs",
    "build:types": "tsc --emitDeclarationOnly --outDir dist",
    "pretest": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "orcid",
    "parser",
    "publication",
    "research"
  ],
  "author": "Sira Pornsiriprasert <code@psira.me> (https://psira.me/)",
  "license": "BSD-3-Clause",
  "homepage": "https://github.com/p-sira/orcid-parser",
  "devDependencies": {
    "@types/node": "^22.19.0",
    "@vitest/coverage-v8": "^4.0.6",
    "esbuild": "^0.25.12",
    "typedoc": "^0.28.14",
    "typescript": "^5.9.3",
    "vitest": "^4.0.6"
  },
  "sideEffects": false,
  "files": [
    "dist"
  ]
}
